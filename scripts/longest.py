class longestWord:

    def find_longest_word(word_list): 

        f = open(longest.txt)
        longest_word =  max(word_list, key=len)
        return longest_word # returns the longest word
        (color.BOLD + longest_word + color.END) # makes color bold for all occurences of the longest word.

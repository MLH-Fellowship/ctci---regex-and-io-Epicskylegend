from distutils.filelist import findall


import re
class filterVowels:
   vowels = ['a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U']

re.findall(vowels in vowels.txt) # find all the instances of vowels inside vowels.txt which we defined above.

print("vowels-" + vowels + ".txt") # print the amount of vowels found in the format given from the problem.
